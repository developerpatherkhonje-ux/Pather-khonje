# Vercel Environment Variables Configuration
# Copy these variables to your Vercel project settings under Environment Variables

# Database Configuration
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/pather-khonje?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-here-change-this-in-production-make-it-long-and-random
JWT_REFRESH_SECRET=your-refresh-token-secret-key-here-also-make-this-long-and-random
JWT_EXPIRE=24h
JWT_REFRESH_EXPIRE=7d

# Security Configuration
BCRYPT_ROUNDS=12
SESSION_SECRET=your-session-secret-key-here-make-this-random-and-long
COOKIE_SECRET=your-cookie-secret-key-here-also-random-and-long

# Admin Users Configuration
ADMIN_EMAIL=admin@patherkhonje.com
ADMIN_PASSWORD=admin@123
ADMIN_NAME=Superadmin

MANAGER_EMAIL=rajdip.mitra@patherkhonje.com
MANAGER_PASSWORD=rajdip@123
MANAGER_NAME=Rajdip Mitra

PROPRIETOR_EMAIL=somashah.mitra@patherkhonje.com
PROPRIETOR_PASSWORD=somashah@123
PROPRIETOR_NAME=Somashah Mitra

# Application Configuration
NODE_ENV=production
FRONTEND_URL=https://pather-khonje.vercel.app
LOG_LEVEL=info

# Security Settings
MAX_LOGIN_ATTEMPTS=5
LOCKOUT_TIME_MS=1800000
PASSWORD_MIN_LENGTH=8
PASSWORD_REQUIRE_UPPERCASE=true
PASSWORD_REQUIRE_LOWERCASE=true
PASSWORD_REQUIRE_NUMBERS=true
PASSWORD_REQUIRE_SYMBOLS=true

# Frontend Environment Variables (for Vite)
VITE_API_URL=/api
VITE_APP_NAME=Pather Khonje
VITE_APP_VERSION=1.0.0

# Instructions:
# 1. Go to your Vercel project dashboard
# 2. Navigate to Settings > Environment Variables
# 3. Add each variable above with its corresponding value
# 4. Make sure to use strong, unique values for all secret keys
# 5. Set NODE_ENV to "production" for production deployments
# 6. Update MONGODB_URI with your actual MongoDB connection string
# 7. Update FRONTEND_URL with your actual frontend domain

